# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Test coverage (generated by `npm run coverage` for fallow / dead-code analysis)
coverage

# Bundled runtimes (downloaded during release build, not committed)
src-tauri/resources/servers/*
!src-tauri/resources/servers/.gitkeep
src-tauri/resources/node/*
!src-tauri/resources/node/.gitkeep
src-tauri/resources/conpty/*
!src-tauri/resources/conpty/.gitkeep
src-tauri/resources/.tmp/

# Claude Code
.claude/*
!.claude/claude.md

# Research journals (persistent-research-policy.md) — local working notes
.research/

# Private design docs — never leave this machine.
# Matched by basename (no leading path) so moving a file between folders can't
# silently re-expose it — the 2026-03-24 docs reorg did exactly that.
DISCORD-SERVER-PLAN.md
clep-*.md
voice-architecture-research.md
CM_Voice_Architecture_Research.md
extension-management-design.md
team-edition-planning.md

# Claude Design sync test output (disposable; not part of the app)
design-sync-test/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Archived per-release sourcemaps (crash-stack symbolication; never shipped)
release-sourcemaps/
.litria/
